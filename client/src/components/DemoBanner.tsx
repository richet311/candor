export function DemoBanner() {
  return (
    <div className="bg-[var(--color-accent-dark)] px-4 py-1.5 text-center text-xs text-white">
      Demo project — donations use Stripe test mode and never reach these organizations.
    </div>
  );
}
